# DISABLED: This workflow fails with permission issues and may not be necessary
# Original workflow was causing HttpError: Resource not accessible by integration
# To re-enable, restore the original content and ensure proper repository permissions

name: Cleanup Old Artifacts (Disabled)

on:
  workflow_dispatch:
    # Only allow manual triggering if needed

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: echo "This workflow is disabled due to permission issues. It was causing HttpError - Resource not accessible by integration." 